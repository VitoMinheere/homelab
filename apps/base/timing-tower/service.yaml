apiVersion: v1
kind: Service
metadata:
  name: timing-tower-service
  namespace: timing-tower
spec:
  type: NodePort
  ports:
    - name: http
      targetPort: 8000
      port: 8000
      nodePort: 30287
      protocol: TCP
  selector:
    app: timing-tower
